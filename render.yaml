services:
  - type: web
    name: prescription-error-detector
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app_gradio.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0